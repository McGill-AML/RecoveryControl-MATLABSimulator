% syms alpha beta R theta wall_loc Cx
syms theta
u = [cos(30*pi/180);0;-sin(30*pi/180)];
n = [sin(30*pi
alpha = 1;
beta = 0;
R = 0.5;
wall_loc = 0.3;
Cx = 0;
S = solve (alpha*R*cos(theta)+beta*R*sin(theta)==wall_loc - Cx,theta);